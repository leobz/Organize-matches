spring:
  main:
    banner-mode: 'off'
  data:
    mongodb:
      database: organize-matches
      auto-index-creation: true

server:
  port: 8081

---
spring:
  config:
    activate:
      on-profile: dev
  data:
    mongodb:
      database: organize-matches-dev

---
spring:
  config:
    activate:
      on-profile: prod
  data:
    mongodb:
      database: organize-matches